eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-Url:
      defaultZone: http://localhost:8761/eureka/
  instance:
         hostname: localhost
spring:
  application:
    name: SUPPLIER-SERVICE
    
server:
  port: 9001